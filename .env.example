# Environment Variables for DevFolio

# GitHub Configuration
# Personal Access Token - Get from: https://github.com/settings/tokens
# Required scopes: public_repo (or 'repo' for private repositories)
REACT_APP_GITHUB_TOKEN=your_github_token_here

# Your GitHub username (for blog post creation)
REACT_APP_GITHUB_USERNAME=your_github_username

# Repository name (usually 'Devfolio' unless you renamed it)
REACT_APP_GITHUB_REPO=Devfolio

# Admin Configuration
# Secure password for accessing the blog creator interface
REACT_APP_ADMIN_PASSWORD=your_secure_admin_password_here

# Instructions:
# 1. Copy this file to .env in your project root
# 2. Replace the placeholder values with your actual values
# 3. To get a GitHub Personal Access Token:
#    - Go to GitHub → Settings → Developer settings → Personal access tokens
#    - Generate a new token with 'repo' permissions
#    - Copy the token and paste it as REACT_APP_GITHUB_TOKEN
# 4. Set a secure admin password for the blog creator interface
# 5. Restart your development server after making changes

# Security Notes:
# - Never commit your .env file to version control
# - Use strong, unique passwords
# - Limit GitHub token permissions to only what's needed
# - Consider using environment-specific configuration for production
